
/**
        // Goes over 00:00AM
        if (beginningTimeHours > endTimeHours) {
            // All cases of night time after 22:00PM
            if (beginningTimeHours >= 22) {
                if (beginningTimeMins == 0){
                    nightTimeHours += 24 - beginningTimeHours;
                }
                else{
                    nightTimeHours += (23 - beginningTimeHours);
                    nightTimeMins += 60 - beginningTimeMins;
                }
            }

            if (endTimeHours >= 22){
                nightTimeHours += (endTimeHours - 22);
                nightTimeMins += endTimeMins;
            }

            if (beginningTimeHours < 22 ) nightTimeHours += 2;

            // All cases of night time before 06:00AM
            if (beginningTimeHours < 6){
                nightTimeHours += 5 - beginningTimeHours;
                nightTimeMins += 60 - beginningTimeMins;
            }
            if (endTimeHours < 6){
                nightTimeHours += endTimeHours;
                nightTimeMins += endTimeMins;
            }
            else{
                nightTimeHours += 6;
            }
        }

        else{
            // Past 22PM
            if (endTimeHours >= 22 && beginningTimeHours < 22){
                nightTimeHours += endTimeHours - 22;
                nightTimeMins += endTimeMins;
            }
            if (endTimeHours >= 22 && beginningTimeHours >= 22){
                nightTimeHours += endTimeHours - beginningTimeHours;
                if (endTimeMins < beginningTimeMins){
                    nightTimeMins += 60 - beginningTimeMins + endTimeMins;
                    nightTimeHours -= 1;
                }
                else {
                    nightTimeMins += endTimeMins;
                    nightTimeMins += 60 - beginningTimeMins;
                }
            }
            // Before 06AM
            if (beginningTimeHours < 6 &&
                (endTimeHours < 6 || (endTimeHours == 6 && endTimeMins == 0))){

                    nightTimeHours += endTimeHours - beginningTimeHours;
                    if (endTimeMins < beginningTimeMins){
                        nightTimeMins += 60 - beginningTimeMins + endTimeMins;
                        nightTimeHours -= 1;
                    }
                    else {
                        nightTimeMins += endTimeMins;
                        nightTimeMins += 60 - beginningTimeMins;
                    }
            }
            if (beginningTimeHours < 6 && endTimeHours >= 6){
                nightTimeHours += 5 - beginningTimeHours;
                nightTimeMins += 60 - beginningTimeMins;

            }

        }
        **/
    // Calculating minutes to hours.
    //alert(nightTimeHours + ":" + nightTimeMins);
